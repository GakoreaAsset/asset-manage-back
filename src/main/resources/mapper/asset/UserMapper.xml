<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kgav.gw.user.mapper.UserMapper">

    <!-- JWT ID조회  -->
    <select id="findById" parameterType="string" resultType="UserModel">
        SELECT userid, userpw, username, common_name as commonName
          FROM userinfo u, common_code c
         WHERE u.roles = c.common_number
           AND userid = #{userid}
    </select>

    <!-- 세션방식 아이디 및 비밀번호 조회  -->
    <select id="getUser" parameterType="UserModel" resultType="Integer">
        SELECT count(*)
          FROM userinfo
         WHERE userid = #{userid}
           AND userpw = #{userpw}
    </select>


    <!-- 아이디 및 비밀번호 생성 -->

    <!-- 아이디 찾기 -->

    <!-- 임시비밀번호 안내 -->

</mapper>



